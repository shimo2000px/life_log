<h2>パスワードリセット</h2>

<%= form_with url: password_resets_path, local: true do |form| %>
  <div class="mb-3">
    <%= form.label :email, 'メールアドレス' %>
    <%= form.email_field :email, class: "form-control" %>
  </div>
  <%= form.submit "リセットリンクを送信", class: "btn btn-primary" %>
<% end %>